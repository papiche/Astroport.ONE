# ZICMAMA laptop — AP WiFi ouvert sur dongle USB dédié (voir zicmama_ap_laptop.sh)
interface=${AP_IFACE}
ssid=${SSID}
hw_mode=g
channel=${CHANNEL}
ieee80211n=1
wmm_enabled=1
# Réseau ouvert — le portail captif est géré par zicmama_ap_portal.py (redirection HTTP totale)
auth_algs=1
max_num_sta=30
